import 'package:flutter/cupertino.dart';

class CustomAssetImageLoad extends StatelessWidget {
  const CustomAssetImageLoad({super.key});

  @override
  Widget build(BuildContext context) {
     return Container();
  }
}
